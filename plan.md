# Plan: eva rebuild

1. Scaffold Rust crate (`Cargo.toml`, `src/main.rs`)
2. Implement parser with implicit multiply and paren balancing
3. Implement evaluator with all functions and angle units
4. Implement output formatter (base + fix)
5. Implement CLI and REPL with history
6. Write `compile.sh`, build, verify against gold binary behaviors
7. Write `review.md`
