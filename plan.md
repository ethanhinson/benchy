# Plan

1. Scaffold `Cargo.toml` and module tree
2. Implement CLI parsing mirroring `--help`
3. Implement bounds parser and complement expansion
4. Implement split (literal, greedy, compress, trim, regex)
5. Implement field/byte/char/line processors and format/json output
6. Wire main loop (stdin or file, line-delimited)
7. Add `compile.sh`, run gold comparison, write `review.md`
